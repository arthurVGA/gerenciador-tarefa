spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop

  datasource:
    url: jdbc:postgresql://localhost:5432/gerenciador-tarefa
    username: postgres
    password: postgres
    driverClassName: org.postgresql.Driver
